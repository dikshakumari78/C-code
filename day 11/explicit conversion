#include <iostream>
using namespace std;

int main() {
    double x = 1.2;
    int sum = (int)x + 1; // Explicit type conversion of 'x' from double to int
    cout << "sum = " << sum;
    return 0;
}
